# tests

T12: schema validation against the synthetic fixture.

T13: job-layout validation, structured failure output, and the exact module command.

T14: official-gate calculations, missing evidence, detection misses/phantoms, photo stitch, head-to-head, and `eval.json` CLI output.

T6: portable RoomPlan JSON parsing, metric surface conversion, LiDAR CLI output, and structured Record3D/USDZ boundaries.

When implementation starts: eval numbers, then recon golden tests. Prefer tiny fixtures over MP4s in CI.

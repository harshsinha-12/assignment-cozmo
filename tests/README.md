# tests

T12: schema validation against the synthetic fixture.

T13: job-layout validation, structured failure output, and the exact module command.

When implementation starts: eval numbers, then recon golden tests. Prefer tiny fixtures over MP4s in CI.

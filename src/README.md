# src

Empty on purpose.

When `docs/takehome.md` is filled, create the package:

```text
src/cozmo_floorplan/
  __init__.py
  cli.py
  io/
  geom/
  recon/
  extract/
  stitch/
  render/
  eval/
```

See `docs/architecture.md`. Do not add placeholder modules that import nothing; they rot and confuse agents.
